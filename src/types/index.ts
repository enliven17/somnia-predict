/* eslint-disable @typescript-eslint/no-explicit-any */`n// src/types/index.ts`n// Central export file for all types`n`n// Market types`nexport * from './market';`n`n// User types  `nexport * from './user';`n`n// Wallet types`nexport * from './wallet';`n`n`n// Common utility types`nexport interface BaseEntity {`n  id: string;`n  createdAt: number;`n  updatedAt: number;`n}`n`nexport interface PaginationParams {`n  page?: number;`n  limit?: number;`n  sortBy?: string;`n  sortOrder?: 'asc' | 'desc';`n}`n`nexport interface FilterParams {`n  search?: string;`n  category?: string;`n  status?: string;`n  dateFrom?: number;`n  dateTo?: number;`n}`n`nexport type LoadingState = 'idle' | 'loading' | 'success' | 'error';`n`nexport interface AsyncState<T> {`n  data: T | null;`n  loading: LoadingState;`n  error: string | null;`n}`n`n// Common error types`nexport interface ErrorResponse {`n  code: string;`n  message: string;`n  details?: Record<string, any>;`n  timestamp: number;`n}`n`n// Component prop helpers`nexport type ComponentSize = 'sm' | 'md' | 'lg';`nexport type ComponentVariant = 'default' | 'outline' | 'ghost' | 'destructive';`nexport type ComponentColor = 'primary' | 'secondary' | 'success' | 'warning' | 'error';`n`n// types/market.ts`nexport interface Market {`n  id: string;`n  title: string;`n  description: string;`n  category: string;`n  optionA: string;`n  optionB: string;`n  creator: string;`n  endTime: string;`n  minBet: string;`n  maxBet: string;`n  status: string;`n  resolved: boolean;`n  totalOptionAShares: string;`n  totalOptionBShares: string;`n  totalPool: string;`n  imageUrl?: string;`n  outcome?: string;`n}`n`nexport interface ResolutionEvidence {`n  marketId: string;`n  creator: string;`n  evidence: string;`n  requestedOutcome: string;`n  submittedAt: string;`n}`n`nexport interface PendingMarketDetails {`n  market: Market;`n  evidence: ResolutionEvidence;`n  totalVolume: string;`n  participantCount: string;`n  daysSinceEnded: string;`n  hasEvidence: boolean;`n}`n`nexport enum MarketOutcome {`n  OptionA = 0,`n  OptionB = 1,`n  Draw = 2,`n  Cancelled = 3,`n}`n`nexport const MarketCategoryLabels: { [key: string]: string } = {`n  "0": "Sports",`n  "1": "Politics",`n  "2": "Entertainment",`n  "3": "Finance",`n  "4": "Technology",`n  "5": "Weather",`n  "6": "Other",`n};